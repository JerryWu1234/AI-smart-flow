# Composite Review-turn hardening closes Host ownership and pause-contract gaps

This follow-up closes every blocker and P2 asymmetry from the immediately preceding review: active composite ownership now gates every compatibility mutation, generic approval pauses distinguish collection from confirmation, inspections are output-only, and primitive repair continuation resets its allowance. The earlier cumulative-path, lost-claim lease, paused-owner, canonical-result, and packaged two-tool repair fixes remain intact. No actionable P0, P1, or P2 remains, so T204 and T205 satisfy their acceptance criteria and are ready to close. **Watch for:** **confirmed, non-blocking evidence gap** T208/T209 remain open because real Pi execution was not authorized and no checked-in redacted transcript exists; **confirmed, P3 bookkeeping gap** task/evidence prose still records the preceding NEEDS_CHANGES state and should be refreshed after this review is accepted.

**Verdict**: APPROVED

## High-level view

Composite ownership is now enforced inside the Project mutation boundary. Public claim, renew, Review submit/unavailable, Leader decision, resume, and cancel calls fail while `run.hostTurn` exists; coordinator calls carry an internal token in the idempotency payload and validate it under the mutation lock, while runs without `hostTurn` retain legacy behavior.

`USER_INPUT_REQUIRED` separates mutable answers from inspections and embeds the canonical paused result. Generic revision approval exposes a null-filled, non-submit-ready `COLLECT` form; a USER repair draft exposes a complete `CONFIRM` answer; public `resume_review_decision` independently resets `autoRepairRounds`.

Lost claim responses reconcile against the committed five-minute lease, pause conversion retains the original Host, and restart/timer paths revalidate durable ownership. Both Host APIs carry complete `changedPaths`, unanswered pauses avoid `smartflow_result`, and the gated packaged scenario enforces `execute → review_turn*`, dynamic repair, Revision 2+, and Reviewer `CREATE → RESUME`.

The packaged path is structurally ready, but the intentionally disabled real-Pi gate and absent sanitized transcript keep T208/T209 open without blocking this code verdict.

<details>
<summary>Issues (2)</summary>

1. **Real-Pi acceptance evidence** (**confirmed**, non-blocking) — T208/T209 lack an authorized real-Pi execution and checked-in redacted transcript. Keep both tasks open until that evidence is produced without exposing credentials or project data.
2. **Stale closure bookkeeping** (**confirmed**, P3/non-blocking) — task and evidence documents still say the now-fixed T204/T205 gaps remain. Mark those tasks complete and refresh the prior-review counts/status only after accepting this final review.

</details>

<details><summary>Details</summary>

## Composite Host authority encloses every compatibility mutation

Each compatibility mutation enters `assertHostTurnAuthority` from inside its `ProjectMutationExecutor` callback: claim at `apps/daemon/src/project-runtime.ts:714-734`, renew at `:749-769`, Review submit at `:788-805`, unavailable reporting at `:819-840`, Leader decision at `:844-867`, resume at `:911-945`, and cancel at `:1188-1195`. The shared guard rejects public calls while an owner exists, validates token/Host mismatches, and returns only when no checkpoint exists at `:1253-1274`, preserving primitive-only legacy runs.

Only `HostTurnCoordinator` can supply the private authority option (`apps/daemon/src/project-runtime.ts:413-428`). It passes the durable token for claim, Review submit/unavailable, every Leader decision, user answers, renewal, and timer-driven failure reporting at `apps/daemon/src/host-turn-coordinator.ts:475-500,681-700,738-759,804-836,980-1003,1346-1377,1482-1504`. Internal options are included in mutation payloads at `apps/daemon/src/project-runtime.ts:718-721,753-756,792-795,824-827,854-857,929-932`; under the lock, the executor hashes that payload before invoking the builder (`apps/daemon/src/project-mutation-executor.ts:107-125,204-205`), so a public reuse of a coordinator child request ID cannot replay its response.

The integration regression proves byte-identical rejection for primitive resume, cancel, and claim at `tests/integration/mcp-lifecycle.test.ts:1156-1277`; the remaining primitives share the same guarded callback. Pause conversion also validates the previous owner, retains its `hostTurnId`, derives a successor token, and persists against the old token at `apps/daemon/src/host-turn-coordinator.ts:852-885`, with wrong-Host coverage at `apps/daemon/src/host-turn-coordinator.test.ts:226-284`.

## Revision collection, confirmation, and inspection have disjoint contracts

Inspection and mutable action sets are disjoint at `packages/protocol/src/schema/mcp-tools.ts:264-282`. `reviewTurnInput.answer` excludes inspections and action-only revision approval, accepting approval only as the complete strict object at `:391-402`; the output uses separate `inspectionOptions` and mutable `options` at `:506-521`.

`COLLECT` requires explicit nullable placeholders and has no answer, while `CONFIRM` requires a complete schema-valid answer at `packages/protocol/src/schema/mcp-tools.ts:404-429`. The coordinator emits both forms and maps inspections separately at `apps/daemon/src/host-turn-coordinator.ts:896-954`; regressions cover generic collection, complete confirmation, and inspection separation at `apps/daemon/src/host-turn-coordinator.test.ts:766-929`. Protocol tests reject action-only approval and inspection answers at `packages/protocol/src/schema/protocol.test.ts:109-211`, and MCP guidance gives matching collect-versus-confirm instructions at `apps/mcp-server/src/server.ts:29-38`.

The same pause output requires canonical `result`. The Host returns it directly when unanswered at `apps/host-skill/src/workflow.ts:67-100`, and the workflow regression excludes primitive `smartflow_result` at `apps/host-skill/src/workflow.test.ts:302-346`.

## Repair allowance, lease recovery, and restart ownership stay durable

Public `ProjectRuntime.resume` now derives the reset from `resumeAction` at `apps/daemon/src/project-runtime.ts:927-928` and writes zero on both resume routes at `:1083-1086,1158-1161`. The legacy primitive regression starts at fifteen and asserts zero after returning to `LEADER_DECISION` at `tests/integration/mcp-lifecycle.test.ts:1469-1494`.

A thrown claim response now triggers a durable reread and reconciliation of matching `REVIEWING + CLAIMING` state rather than a second claim (`apps/daemon/src/host-turn-coordinator.ts:470-545`). Wake times use `claimExpiresAt - 30s` at `:1186-1225`; the regression commits a five-minute lease and receives `REVIEW_REQUIRED` without pausing at `apps/daemon/src/host-turn-coordinator.test.ts:608-657`.

Restart invokes Host-turn recovery before rereading state and skips legacy scheduling while a checkpoint remains at `apps/daemon/src/project-runtime.ts:521-524`. Timer callbacks serialize by run, revalidate the current token, and are cleared on disposal at `apps/daemon/src/host-turn-coordinator.ts:302-307,1227-1246,1338-1378`.

## Cumulative paths and packaged dynamic repair remain intact

The Reviewer contract carries `changedPaths` at `apps/host-skill/src/reviewer.ts:13-21`; high-level and compatibility paths copy the full array at `apps/host-skill/src/workflow.ts:103-111` and `apps/host-skill/src/action-loop.ts:268-277`, with multi-path `CREATE/RESUME` coverage at `apps/host-skill/src/workflow.test.ts:247-295` and `apps/host-skill/src/action-loop.test.ts:89-128`.

The installed child imports packaged `executeApprovedWorkflow`, permits only initial execute plus review-turn calls, derives a dynamic marker from `reviewAttemptId`, and records Reviewer modes and paths at `tests/fixtures/installed-mcp-lifecycle-child.mjs:119-244`. The outer test requires Revision 2+, no primitive result fallback, at least two Reviews, stable `CREATE → RESUME`, cumulative `sum.js`, and the published marker at `tests/e2e/installed-package.test.ts:337-380,421-432`. Its real-Pi gate remains explicit at `tests/e2e/installed-package.test.ts:11,143`.

## T204/T205 are ready; T208/T209 are not

T204’s criteria at `specs/001-smartflow-mvp/tasks.md:241` are met by locked CAS, stable child IDs, stale-continuation handling, owner-preserving pauses, lost-claim reconciliation, restart-first recovery, and lease-aware timers. **Task readiness: T204 is ready to mark complete.**

T205’s criteria at `specs/001-smartflow-mvp/tasks.md:242` are met by complete callback paths, same-session `CREATE/RESUME`, self-contained pause results, separated inspections, structured approval input, and an execute/review-turn-only high-level workflow. **Task readiness: T205 is ready to mark complete.**

The old status remains in `specs/001-smartflow-mvp/tasks.md:233,241-255`, `specs/001-smartflow-mvp/reviewer-loop-tasks.md:146-149`, `specs/001-smartflow-mvp/research.md:110-113`, and `specs/001-smartflow-mvp/quickstart.md:97-100`; this is the **confirmed, P3/non-blocking** bookkeeping item above. T208/T209 remain open under `specs/001-smartflow-mvp/tasks.md:245-246`: the **confirmed, non-blocking evidence gap** is absent real installed-Pi/endpoint evidence and a checked-in redacted transcript, not a demonstrated code failure.

## Validation stayed offline

Provided evidence reports typecheck, targeted ESLint, unit 115/115, contract 9/9, integration 45/45, security 8/8, crash 24/24, E2E 15 passed with one explicitly gated real-Pi skip, build with only the known pre-existing task-manifest unresolved-import warning, and `git diff --check`. This pass independently reran the coordinator, protocol, Host workflow/action-loop, and MCP lifecycle tests: 56/56 passed; `git diff HEAD --check` also passed. No model, network, Pi, or external endpoint was invoked.

</details>

<details>
<summary>File map</summary>

- `.specify/memory/constitution.md` — records daemon-owned Review-turn and durable ownership boundaries.
- `README.md` — documents the preferred two-tool flow.
- `SmartFlow-Spec-Kit-R5.md` — updates the architecture and evidence boundary.
- `apps/daemon/src/host-turn-coordinator.ts` — implements composite orchestration, ownership, recovery, timers, decisions, and pauses.
- `apps/daemon/src/host-turn-coordinator.test.ts` — covers CAS, ownership, lease recovery, approval forms, and inspection output.
- `apps/daemon/src/index.ts` — exports the coordinator.
- `apps/daemon/src/main.ts` — disposes coordinator timers on shutdown.
- `apps/daemon/src/project-runtime.ts` — gates compatibility mutations and wires composite recovery and repair counters.
- `apps/host-skill/src/action-loop.ts` — retains the primitive compatibility loop and forwards changed paths.
- `apps/host-skill/src/action-loop.test.ts` — covers legacy callback context and leases.
- `apps/host-skill/src/reviewer.ts` — adds cumulative paths to the Reviewer callback contract.
- `apps/host-skill/src/workflow.ts` — drives execute/review-turn and consumes embedded pause results.
- `apps/host-skill/src/workflow.test.ts` — covers repair, session reuse, paths, pause return, and structured answers.
- `apps/mcp-server/src/server.ts` — registers and instructs the composite MCP flow.
- `apps/mcp-server/src/tools/index.ts` — exports the review-turn handler.
- `apps/mcp-server/src/tools/review-turn.ts` — validates and forwards composite calls.
- `packages/protocol/src/schema/mcp-tools.ts` — defines composite states, answer forms, inspection options, and result schemas.
- `packages/protocol/src/schema/protocol.test.ts` — covers strict four-state and answer unions.
- `packages/provider-pi/src/mcp-model-extension.ts` — validates and registers the self-contained Pi provider extension.
- `packages/provider-pi/src/mcp-model-extension.test.ts` — covers API variants, safe integers, and secret indirection.
- `packages/review/src/index.ts` — exports deterministic Review planning.
- `packages/review/src/review-decision.ts` — maps validated Reviews to accept, repair, or pause.
- `packages/review/src/review-decision.test.ts` — covers policy and the fifteen-round boundary.
- `packages/state-store/src/schema.ts` — persists Host turns and repair counts.
- `packages/state-store/src/schema.test.ts` — validates the durable fields.
- `packages/state-store/src/test-fixture.ts` — initializes the repair counter.
- `pnpm-workspace.yaml` — updates native dependency build allow-list entries.
- `semantic-review/2026-08-11-191436-pr-0.md` — preserves the first prior semantic review.
- `semantic-review/2026-08-12-111310-pr-0.md` — preserves the second prior semantic review.
- `specs/001-smartflow-mvp/adr-auto-review-loop.md` — supersedes the older Host-owned loop.
- `specs/001-smartflow-mvp/adr-daemon-owned-review-turn.md` — records the daemon-owned design.
- `specs/001-smartflow-mvp/checklists/requirements.md` — tracks composite safety and evidence gates.
- `specs/001-smartflow-mvp/contracts/pi-worker.md` — clarifies Pi/Reviewer identity and evidence obligations.
- `specs/001-smartflow-mvp/contracts/review-turn.md` — specifies states, ownership, CAS, timers, and tool count.
- `specs/001-smartflow-mvp/contracts/run-concurrency.md` — defines Project CAS and Host ownership.
- `specs/001-smartflow-mvp/data-model.md` — documents HostTurn, result unions, and repair count.
- `specs/001-smartflow-mvp/implementation-map.md` — maps requirements to code and evidence.
- `specs/001-smartflow-mvp/plan.md` — updates architecture and validation strategy.
- `specs/001-smartflow-mvp/quickstart.md` — documents two-tool, restart, pause, and evidence checks.
- `specs/001-smartflow-mvp/research.md` — records rationale and open evidence.
- `specs/001-smartflow-mvp/review-loop-glossary.md` — updates Review-turn terminology.
- `specs/001-smartflow-mvp/reviewer-loop-tasks.md` — defines session reuse and current gap tracking.
- `specs/001-smartflow-mvp/spec.md` — adds composite requirements and success criteria.
- `specs/001-smartflow-mvp/tasks.md` — tracks T201–T210 and evidence gates.
- `tests/contract/mcp-v5.test.ts` — updates the public contract to eleven tools.
- `tests/e2e/installed-package.test.ts` — gates the installed real-Pi two-tool repair/Publish scenario.
- `tests/e2e/production-repair-loop.test.ts` — covers production-composition recovery, repair limit, and Publish.
- `tests/fixtures/installed-mcp-lifecycle-child.mjs` — runs the packaged two-tool dynamic-repair fixture.
- `tests/integration/mcp-lifecycle.test.ts` — covers compatibility ownership guards and primitive budget reset.

The immediately preceding report, `semantic-review/2026-08-12-140620-pr-0.md`, was untracked review context and was read before this pass. Full tracked staged-plus-unstaged diff: `git diff HEAD`. Worktree inventory: `git status --short`.

</details>
