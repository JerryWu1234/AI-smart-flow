# 应用层 `schemaVersion` latest-only 最终复核

本轮以 staged index 为基线，只复核 unstaged working-tree diff 与 `.changeset/remove-application-schema-versions.md`。前次两项 P2 已关闭：SQLite layout 4 现在有拒绝且不改写版本的回归测试，daemon-driven Review 文档也已改为无应用版本、SQLite layout 5 与清理旧 Data Directory。应用对象、fresh build 产物、哈希/恢复/发布链路和 CAS 未发现实现故障，但实现计划开头仍把 ProjectState 的当前应用版本写成 7，与本次最终契约冲突。该发布文档矛盾修正前，本轮不能批准。

Watch for: **confirmed（P2）** `实现计划.md` 仍宣称 ProjectState 当前应用版本是 7，应改为“移除前为 7、最终无应用版本”或删除该表述。

**Verdict**: NEEDS_CHANGES

## High-level view

应用层 producer、schema 与 verifier 已统一到无版本 ProjectState、TaskManifest、Git snapshot、Candidate、Review/Leader decision 和 Doctor JSON。Fresh build 后对 `apps/*/dist`、`packages/*/dist` 与顶层 `dist` 的扫描没有发现 `schemaVersion`、`GitCandidateV3` 或 `document_schema_version`；源码和测试中的 `schemaVersion` 命中只剩明确的不存在断言。

SQLite 使用 `DATABASE_SCHEMA_VERSION = 5`，只接受 `user_version` 0 或 5；新回归测试证明 version 4 返回 `STATE_MIGRATION_UNSUPPORTED` 且保留 4，没有 DDL 或 migration 改写。Candidate → Review → Leader semantic hash、artifact SHA、recovery binding、state/fence/generation 与 publish file-level CAS 仍在，publish operation ID 继续只由 `projectId`、`jobId`、`candidateHash`、`reviewHash` 和 `operationsHash` 构成。两轮定向验证合计 102 个测试通过、1 个条件性用例跳过，typecheck、lint、完整 build、release check 与 `git diff --check` 均通过。

变更范围都围绕 latest-only 断代及其 fixture、测试和发布说明，但 `实现计划.md:3` 的“ProjectState 当前应用版本是 7”仍以现在时描述已被删除的契约。这是 **confirmed（P2）** 文档冲突，且与同一文件后续的无版本最终契约、修正后的 changeset 和 daemon Review 文档不一致。

<details>
<summary>Issues (1)</summary>

1. **实现计划仍称 ProjectState 当前为 v7（P2, confirmed）** — `实现计划.md:3` 与最终无版本契约冲突；改为“移除前为 7、最终无应用版本”或删除该子句。

</details>

<details>
<summary>Details</summary>

## 实现计划仍把已删除的 v7 称为当前契约

`实现计划.md:3` 新增的状态说明写着“ProjectState 当前应用版本是 7”，而同一变更已经从 `projectStateSchema`、初始化器和 StateStore parser 删除该字段，文件后续也把最终对象定义为无版本。这是 **confirmed（P2）** 的当前态矛盾；应将该句明确改成“移除前的应用版本为 7，最终契约无应用版本”，或直接删除版本 7 子句，避免发布和清库说明继续暗示存在可识别的 ProjectState v7。

</details>

<details>
<summary>File map</summary>

- `.changeset/persistent-pi-session-repair-loop.md` — 使早期 repair-loop 发布说明服从后续无版本断代。
- `.changeset/remove-dead-code-state-v7.md` — 将 ProjectState-v7/layout-4 说明改为无版本 state/layout 5。
- `.changeset/remove-application-schema-versions.md` — 新增 breaking cutover、清库要求与保留的安全身份说明。
- `apps/cli/src/doctor.ts` — 删除 DoctorReport 版本字段及输出值。
- `apps/daemon/src/git-publish-source.ts` — 删除 Candidate-v3 guard，保留 snapshot binding。
- `apps/daemon/src/project-runtime.ts` — 初始化无版本 ProjectState。
- `apps/daemon/src/review-coordinator.ts` — 对无版本 Review/Leader payload 哈希并持久化。
- `docs/plans/daemon-driven-review.md` — 将当前 v7 表述改为无版本 state/layout 5 与清库升级。
- `docs/plans/dead-code-audit-and-cleanup.md` — 记录后续断代取代 v7/layout-4 决策。
- `docs/plans/immutable-job-revision-removal.md` — 区分历史 Revision 移除与后续无版本断代。
- `docs/plans/persistent-pi-session-review-loop.md` — 对齐 artifact 与 recovery 的最终契约。
- `packages/protocol/src/schema/run-state.ts` — 从 strict durable Review/Leader schema 删除版本 literal。
- `packages/state-store/src/schema.ts` — 删除 ProjectState 应用格式版本。
- `packages/state-store/src/state-store.ts` — 升级 SQLite layout 5，删除文档版本镜像列与对账。
- `packages/task-manifest/src/schema.ts` — 删除 TaskManifest 版本 literal。
- `packages/task-manifest/src/task-manifest.ts` — 生成并哈希最终无版本 manifest。
- `packages/workspace/src/candidate-builder.ts` — 用直接定义的 Candidate 取代 GitCandidateV3 与 alias。
- `packages/workspace/src/git-snapshot.ts` — 生成和验证无版本 snapshot。
- `tests/crash/atomic-state.test.ts` — 使直接 SQLite crash writer 匹配五列 state table。
- `tests/crash/recovery-test-fixture.ts` — 以无版本 payload 重建恢复证据链。
- `tests/fixtures/state-store/test-fixture.ts` — 构造无版本 ProjectState。
- `tests/integration/pi-runner.test.ts` — 断言 Candidate artifact 无版本字段。
- `tests/integration/workspace-candidate.test.ts` — 删除 Candidate-v3 shape 期望。
- `tests/unit/apps/cli/doctor.test.ts` — 断言 Doctor JSON 无版本字段。
- `tests/unit/apps/daemon/git-publish-source.test.ts` — 更新 Candidate/snapshot fixture。
- `tests/unit/packages/protocol/schema/protocol.test.ts` — 覆盖 strict 无版本 Review/Leader schema。
- `tests/unit/packages/review/review-prompt.test.ts` — 更新 manifest fixture。
- `tests/unit/packages/state-store/schema.test.ts` — 检查 fresh layout 5，并覆盖 layout 4 拒绝且不改写。
- `tests/unit/packages/task-manifest/task-manifest.test.ts` — 断言无版本 deterministic manifest。
- `tests/unit/packages/workspace/control-plane-paths.test.ts` — 更新 snapshot/Candidate fixture。
- `tests/unit/packages/workspace/git-candidate.test.ts` — 断言直接定义的四键 Candidate shape。
- `实现计划.md` — 修正最终契约、Candidate 命名、SQLite 守卫名与 publish identity，但状态行仍残留当前 v7 表述。

Full diff: `git diff`；未跟踪 changeset: `git diff --no-index /dev/null .changeset/remove-application-schema-versions.md`。

</details>
