# Changesets

Run `pnpm changeset` for a pull request that changes the published CLI.
Select `@smartflow/cli`; it is the private version adapter for the single
published package, `@jerrywu1234/smartflow`.

Documentation, tests, and CI-only changes do not need a changeset.
