---
"@smartflow/cli": patch
---

Remove two composition seams nothing supplies (`workspaceApplyAdapter` and the no-progress-threshold override) and the unread `config` and `dataDirectory` fields on the Daemon controller.
