---
"@smartflow/cli": major
---

Expose the Worker reasoning effort as `WORK_EFFORT` in each MCP server environment. It accepts `off`, `minimal`, `low`, `medium`, `high`, `xhigh` or `max` and defaults to `high`. The same name carries through to the Worker, so there is no separate internal thinking-level variable.
