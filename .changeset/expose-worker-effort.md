---
"@smartflow/cli": major
---

Expose optional `EFFORT` in each MCP server environment and normalize it to the Worker's internal Pi thinking level. MCP configurations must replace `SMARTFLOW_PI_THINKING` with `EFFORT`; the former name is now reserved for daemon-to-Worker transport.
